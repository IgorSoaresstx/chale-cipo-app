# Regras do Claude — Chalé Serra do Cipó

Antes de iniciar qualquer tarefa, leia `PROJECT_RULES.md` e `docs/AI_HANDOFF.md`.

O arquivo `PROJECT_RULES.md` contém as regras compartilhadas entre Claude e Codex. Este arquivo existe para que o Claude carregue essas regras automaticamente.

Ao concluir ou interromper um trabalho, atualize `docs/AI_HANDOFF.md` com o estado real e verificável da tarefa.
